---
description: Upstream gating requirements — blocks forbidden vocabulary in docs and manifests
alwaysApply: true
---

# Upstream gate requirements

Re-check before every release tag. Run `./scripts/gate-check.sh`.

| # | Rule |
|---|------|
| 1 | No host product identity in committed docs |
| 2 | No family / host adapter crate names in upstream manifests |
| 3 | Hero README leads with `spectra_schema!` / `spectra_metric!` |
| 4 | No maintainer sync / vendor mirror guides in upstream |
| 5 | Hosts consume via git tag pin |
| 6 | Use **embedded** / **remote** only — no global Mode enum |
| 7 | Storage on `Spectra::builder()` — not a Mode enum |
| 8 | No host DB engine | No host-only embedded DB backend or engine SDK in upstream |
| 9 | No `DESIGN.md` — architecture lives in README + rustdoc |

When porting from a host workspace in Phase 1+, strip host-only DB storage, product topic scans, and host-specific sink types before committing.
